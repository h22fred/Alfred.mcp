{
  "name": "alfred-mcp",
  "version": "1.0.0",
  "description": "AlFred.mcp — Every superhero needs an Alfred. Yours logs the Engagements.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "hono": "^4.12.21",
    "playwright": "^1.60.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "typescript": "^5.5.0",
    "vitest": "^4.1.7"
  },
  "overrides": {
    "fast-uri": ">=3.1.2"
  }
}
